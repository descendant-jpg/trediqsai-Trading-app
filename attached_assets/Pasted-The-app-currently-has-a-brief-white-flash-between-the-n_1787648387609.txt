The app currently has a brief white flash between the native splash screen and the home screen. I want to eliminate this flash and replace the standard loading transition with a premium, custom animated boot screen that highlights our features.

Please execute the following step-by-step:

1. **Eradicate the White Flash:**
   - Update `app.json` to set the `backgroundColor` under the `splash` object (and `android`/`ios` objects if necessary) to our app's dark background hex code (e.g., `#0F172A` or `#000000`) instead of white.
   - Use `expo-splash-screen` to `preventAutoHideAsync()` in `_layout.tsx`, and only `hideAsync()` once the custom animated component is fully mounted and ready to render.
   - Ensure the root `<View>` of the app is wrapped in the dark background color.

2. **Build the Custom Animated Boot Screen:**
   - Create a new component (e.g., `AnimatedBootScreen`) that renders immediately after the native splash screen is hidden.
   - Using `react-native-reanimated` (install it if missing), create a premium, smooth 60fps animation sequence:
     * First, fade in the "TradiQs AI" logo/text centered on the screen.
     * Next, stagger the fade-in and slide-up of the following feature text underneath it: "AI-Powered Trading", "Forex, Crypto & Stocks", "Multi-TF Bias Analysis", and "AutoPilot Execution".
     * Apply a premium styling to this text (using the app's teal/purple gradient colors or clean white/gray).
   - Integrate `expo-haptics` to trigger a light impact vibration as each feature text animates in (guarded for web).

3. **The Seamless Handoff:**
   - Once the animation sequence finishes (approx 2.5 to 3 seconds total), gracefully fade out the `AnimatedBootScreen` component entirely, allowing the main Expo Router navigation (the `(tabs)` home screen) to be revealed seamlessly without any re-renders or flashes.

4. **Verify & Deploy:**
   - Run a strict `tsc --noEmit` typecheck to ensure all animation libraries and imports are correct.
   - Verify the screen renders flawlessly in the preview without flashing white.
   - Commit and push the changes to the GitHub `main` branch.