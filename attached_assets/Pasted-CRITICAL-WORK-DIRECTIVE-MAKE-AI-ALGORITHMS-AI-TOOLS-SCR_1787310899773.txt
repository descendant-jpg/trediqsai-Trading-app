CRITICAL WORK DIRECTIVE: MAKE AI ALGORITHMS (AI TOOLS) SCREEN FULLY FUNCTIONAL AND INTERACTIVE.

To conserve context window and AI credits, DO NOT read previous chat history. Read `replit.md` to understand the current state.

The "AI Tools" / Algorithms screen currently displays static cards where the switches and gear settings buttons are non-functional. Make this screen fully interactive with persistent state, configuration modals, and tier-gating paywalls.

### 1. Interactive Algorithm State & Persistence
- Locate the AI Tools screen component (e.g. `client/src/screens/AIToolsScreen.tsx` or `algorithms.tsx`).
- Implement state management for each algorithm:
  - `status`: `'active' | 'paused'`
  - `settings`: `{ allocation: number, maxDrawdown: number, riskLevel: string, selectedPairs: string[] }`
- Persist algorithm states using `AsyncStorage` (or backend user state) so toggled bots remain active across app restarts.
- **Visual Feedback:**
  - When toggled ON: Switch turns cyan/green, status label changes to a pulsing green dot with "🟢 RUNNING", and card border gets a subtle glow.
  - When toggled OFF: Switch turns gray, status label changes to "⚪ PAUSED".

### 2. Subscription & Tier Gating
- If a user on the **Free tier** attempts to toggle an algorithm marked **PRO ONLY** (e.g., "Quantum Institutional AI"):
  - Prevent toggle activation.
  - Open the native Upgrade / Paywall modal with a tailored message: *"Upgrade to Pro to deploy Institutional Order Flow bots."*
- If a Free user attempts to activate more than 1 algorithm simultaneously:
  - Prevent activation and prompt an upgrade modal: *"Free tier allows 1 active AI bot. Upgrade to activate unlimited bots."*

### 3. Algorithm Settings Modal (⚙️ Gear Icon & Card Tap)
- Tapping the gear icon ⚙️ (or tapping the card header) opens a modern bottom-sheet or modal with customizable bot parameters:
  - **Allocation Size:** Slider or input ($500 to $50,000).
  - **Max Drawdown Limit:** Step slider (3%, 5%, 10%, 15%).
  - **Trading Style / Execution:** Selector chips (`Conservative`, `Balanced`, `Aggressive`).
  - **Asset Allocation:** Toggleable chips (e.g. `BTC/USD`, `ETH/USD`, `SOL/USD`, `EUR/USD`).
  - **Save & Apply Button:** Updates the algorithm config state and closes the modal with a success toast.

### 4. Live Bot Activity & Simulated Feed
- Add a "Live Execution Stream" or "Recent Bot Signals" section below the algorithms (or collapsible inside each card) showing recent signals generated by the active bots (e.g., *"Scalp Oracle bought BTC at $... — +1.4% TP hit"*).

### 5. Verification & Push
- Run full workspace typechecks across client and server.
- Commit: `feat(ai-tools): implement interactive algorithm toggles, configuration modals, and tier gating`
- Push securely to the active repository.

EXECUTION REQUIREMENT: Detail the component changes made, confirm modal interactions work, and verify the Git push was successful.