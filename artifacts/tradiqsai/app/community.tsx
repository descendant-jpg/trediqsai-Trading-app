import React from 'react';
import { Linking, Platform, ScrollView, StyleSheet, Text, TouchableOpacity, View } from 'react-native';
import { Feather } from '@expo/vector-icons';
import { useRouter } from 'expo-router';
import { useSafeAreaInsets } from 'react-native-safe-area-context';
import colors from '@/constants/colors';
const c = colors.light;
export default function CommunityScreen() { const router = useRouter(); const insets = useSafeAreaInsets(); const openTelegram = () => Linking.openURL('https://t.me/tradiqsai').catch(() => { if (Platform.OS === 'web') window.alert('Unable to open Telegram right now.'); }); return <View style={s.container}><ScrollView contentContainerStyle={[s.content, { paddingTop: Platform.OS === 'web' ? 20 : insets.top + 10 }]}><View style={s.header}><TouchableOpacity onPress={() => router.back()} accessibilityRole="button" accessibilityLabel="Go back"><Feather name="chevron-left" size={23} color={c.foreground} /></TouchableOpacity><Text style={s.title}>Community</Text><Feather name="users" size={21} color={c.primary} /></View><View style={s.article}><View style={s.oracle}><Feather name="cpu" size={18} color={c.primary} /><Text style={s.oracleText}>TradiQsAI Oracle</Text></View><Text style={s.articleTitle}>AutoCopy access, explained</Text><Text style={s.body}>AutoCopy is designed for traders who are ready to demonstrate consistency, not chase shortcuts.</Text></View><Text style={s.section}>REQUIREMENTS</Text>{['Register with a verified broker partner', 'Complete the 30-trading-lot requirement', 'Maintain responsible risk and account activity'].map((item) => <View key={item} style={s.check}><Feather name="check-circle" size={19} color={c.success} /><Text style={s.checkText}>{item}</Text></View>)}<View style={s.breakdown}><Text style={s.section}>WHY IS THIS REQUIRED?</Text><Text style={s.body}>Broker registration confirms an eligible trading environment. The 30-lot threshold gives the system enough history to assess execution quality, risk discipline, and consistency before AutoCopy access is enabled.</Text></View><TouchableOpacity style={s.cta} onPress={openTelegram} accessibilityRole="button" accessibilityLabel="Join verified Telegram community"><Feather name="send" size={17} color={c.primaryForeground} /><Text style={s.ctaText}>Join Verified Telegram Community</Text></TouchableOpacity></ScrollView></View>; }
const s = StyleSheet.create({ container: { flex: 1, backgroundColor: c.background }, content: { padding: 18, gap: 13 }, header: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center' }, title: { color: c.foreground, fontSize: 22, fontFamily: 'Inter_700Bold' }, article: { backgroundColor: c.card, borderWidth: 1, borderColor: c.primary, borderRadius: 16, padding: 20, gap: 12, marginTop: 14 }, oracle: { flexDirection: 'row', alignItems: 'center', gap: 7 }, oracleText: { color: c.primary, fontFamily: 'Inter_700Bold', fontSize: 12 }, articleTitle: { color: c.foreground, fontSize: 21, fontFamily: 'Inter_700Bold' }, body: { color: c.mutedForeground, fontSize: 14, lineHeight: 22 }, section: { color: c.mutedForeground, fontSize: 10, letterSpacing: 1.2, fontFamily: 'Inter_700Bold', marginTop: 8 }, check: { flexDirection: 'row', alignItems: 'center', gap: 11, backgroundColor: c.card, borderColor: c.border, borderWidth: 1, borderRadius: 11, padding: 14 }, checkText: { color: c.foreground, flex: 1, fontSize: 14, fontFamily: 'Inter_600SemiBold' }, breakdown: { backgroundColor: c.card, borderColor: c.border, borderWidth: 1, borderRadius: 13, padding: 16, gap: 9, marginTop: 8 }, cta: { backgroundColor: c.primary, borderRadius: 12, padding: 16, flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 8, marginTop: 8 }, ctaText: { color: c.primaryForeground, fontFamily: 'Inter_700Bold', fontSize: 14 } });