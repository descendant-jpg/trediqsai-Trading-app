import React, { useState } from 'react';
import { Platform, ScrollView, StyleSheet, Text, TouchableOpacity, View } from 'react-native';
import { Feather } from '@expo/vector-icons';
import { useRouter } from 'expo-router';
import { useSafeAreaInsets } from 'react-native-safe-area-context';
import colors from '@/constants/colors';
const c = colors.light; const FILTERS = ['All', 'Indicators', 'Bots', 'Videos'] as const;
export default function ShopScreen() { const router = useRouter(); const insets = useSafeAreaInsets(); const [filter, setFilter] = useState<typeof FILTERS[number]>('All'); return <View style={s.container}><ScrollView contentContainerStyle={[s.content, { paddingTop: Platform.OS === 'web' ? 20 : insets.top + 10 }]}><View style={s.header}><TouchableOpacity onPress={() => router.back()} accessibilityRole="button" accessibilityLabel="Go back"><Feather name="chevron-left" size={23} color={c.foreground} /></TouchableOpacity><Text style={s.title}>TradiQsAI Shop</Text><Feather name="shopping-bag" size={21} color={c.primary} /></View><View style={s.hero}><Feather name="shopping-bag" size={30} color={c.primary} /><Text style={s.heroTitle}>Indicators, Bots, Masterclasses</Text><Text style={s.body}>Tools built for a more disciplined trading process.</Text></View><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={s.filters}>{FILTERS.map((item) => <TouchableOpacity key={item} onPress={() => setFilter(item)} style={[s.pill, filter === item && s.active]} accessibilityRole="button" accessibilityLabel={`Filter ${item}`}><Text style={[s.pillText, filter === item && s.activeText]}>{item}</Text></TouchableOpacity>)}</ScrollView><View style={s.empty}><Feather name="package" size={38} color={c.mutedForeground} /><Text style={s.emptyTitle}>Coming Soon</Text><Text style={s.body}>The TradiQsAI marketplace is under construction.</Text><Text style={s.filterNote}>Showing: {filter}</Text></View></ScrollView></View>; }
const s = StyleSheet.create({ container: { flex: 1, backgroundColor: c.background }, content: { padding: 18, gap: 16 }, header: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center' }, title: { color: c.foreground, fontSize: 21, fontFamily: 'Inter_700Bold' }, hero: { backgroundColor: c.card, borderColor: c.primary, borderWidth: 1, borderRadius: 16, padding: 22, gap: 11, marginTop: 12 }, heroTitle: { color: c.foreground, fontSize: 21, fontFamily: 'Inter_700Bold' }, body: { color: c.mutedForeground, fontSize: 14, lineHeight: 21 }, filters: { gap: 8 }, pill: { paddingHorizontal: 15, paddingVertical: 9, backgroundColor: c.card, borderRadius: 18 }, active: { backgroundColor: c.primary }, pillText: { color: c.mutedForeground, fontSize: 12, fontFamily: 'Inter_700Bold' }, activeText: { color: c.primaryForeground }, empty: { alignItems: 'center', justifyContent: 'center', minHeight: 260, gap: 10, borderWidth: 1, borderColor: c.border, borderRadius: 16, backgroundColor: c.card }, emptyTitle: { color: c.foreground, fontSize: 20, fontFamily: 'Inter_700Bold' }, filterNote: { color: c.primary, fontSize: 11, fontFamily: 'Inter_600SemiBold' } });