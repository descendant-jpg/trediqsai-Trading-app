export default async function Article({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; return <main className="mx-auto max-w-3xl px-5 py-24 lg:px-8"><p className="text-xs font-bold uppercase tracking-[.25em] text-cyan">Market structure · 6 min read</p><h1 className="mt-5 text-5xl font-black tracking-tight">Reading the market with a clearer lens</h1><p className="mt-6 text-lg leading-8 text-muted">A practical field note from the TradiQs AI research desk.</p><article className="mt-14 space-y-6 text-base leading-8 text-white/75"><p>Markets reward preparation, not prediction. Before a trade exists, map where liquidity is likely resting and what would invalidate the idea.</p><h2 className="pt-5 text-2xl font-bold text-white">Start with context</h2><p>Use structure, momentum, and session behavior together. A single indicator is a clue; a complete thesis is a system.</p><p className="rounded-2xl border border-cyan/20 bg-cyan/5 p-6 text-cyan">This article is rendered from mock CMS data. The content model is ready to connect to Supabase.</p></article><p className="mt-12 text-xs text-muted">CMS slug: {slug}</p></main>; }