'use client';
import { useState } from 'react';
import { Check, Mail, Send } from 'lucide-react';
export default function Inbox() { const [selected, setSelected] = useState(0); const [replied, setReplied] = useState(false); const messages = [{ name: 'Maya Chen', subject: 'Partnership inquiry', preview: 'We would love to discuss...', time: '10m' }, { name: 'Alex Morgan', subject: 'Unable to access signals', preview: 'I upgraded this morning...', time: '1h' }, { name: 'Jordan Reed', subject: 'App feedback', preview: 'The new terminal is...', time: '3h' }]; return <div className="p-5 md:p-8 lg:p-10"><p className="text-xs font-bold uppercase tracking-[.25em] text-cyan">Support operations</p><h1 className="mt-3 text-3xl font-black">Help Desk</h1><div className="mt-8 grid min-h-[560px] overflow-hidden rounded-2xl border border-white/10 bg-card lg:grid-cols-[320px_1fr]">{<div className="border-b border-white/10 lg:border-b-0 lg:border-r">{messages.map((m, i) => <button onClick={() => { setSelected(i); setReplied(false); }} className={`w-full border-b border-white/10 p-5 text-left ${selected === i ? 'bg-cyan/10' : ''}`} key={m.name}><div className="flex justify-between"><p className="font-semibold">{m.name}</p><span className="text-xs text-muted">{m.time}</span></div><p className="mt-2 text-sm text-cyan">{m.subject}</p><p className="mt-1 truncate text-xs text-muted">{m.preview}</p></button>)}</div>}<div className="p-6"><div className="flex items-center gap-3 border-b border-white/10 pb-5"><span className="flex h-10 w-10 items-center justify-center rounded-full bg-cyan/10 text-cyan"><Mail className="h-4 w-4" /></span><div><h2 className="font-bold">{messages[selected].subject}</h2><p className="text-xs text-muted">From {messages[selected].name} · support request</p></div></div><p className="mt-8 max-w-xl text-sm leading-7 text-muted">Hi team, I wanted to reach out about my TradiQs AI experience. Could someone from the team help me understand the next steps?</p><div className="mt-12 border-t border-white/10 pt-5"><textarea className="min-h-28 w-full rounded-xl border border-white/10 bg-ink p-3 text-sm outline-none focus:border-cyan" placeholder="Write a reply..." /><button onClick={() => setReplied(true)} className="mt-3 flex items-center gap-2 rounded-xl bg-cyan px-4 py-3 text-sm font-bold text-ink">{replied ? <><Check className="h-4 w-4" /> Reply sent</> : <><Send className="h-4 w-4" /> Send reply</>}</button></div></div></div></div>; }