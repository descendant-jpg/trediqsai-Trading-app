CRITICAL FORCE WORK DIRECTIVE: YOU ARE FORCED TO IMPLEMENT FULL, COMPLETE, PRODUCTION-READY UI AND ENFORCE STRICT PROP FIRM EVALUATION LOGIC. DO NOT LEAVE LOOPHOLES OR HARDCODED DUMMY TEXT. 

Review replit.md for our project rules. We are overhauling the Payout UI on the Profile screen (`app/(tabs)/profile.tsx` or `app/profile.tsx`) to enforce a sustainable 5%/10% virtual-to-real cash conversion model and strict risk guardrails to protect the company treasury.

### 1. Refactor the Payout UI 
- Locate the card displaying "SIMULATED EQUITY" and the "Request Payout" button.
- Change the main title from "SIMULATED EQUITY" to "REAL CASHOUT VALUE".
- Underneath this, display the exact math calculation so it is legally clear to the user:
  - Starting Demo Balance: `$10,000.00`
  - Virtual Profit: `[Calculated virtual profit]`
  - Profit Split: `5% (Pro)` OR `10% (Elite)`
- The large dollar amount displayed next to the button must ONLY show the calculated Real Cashout Value (Virtual Profit * Split %).

### 2. Implement the Payout Cap Logic
- Ensure the calculated Real Cashout Value never exceeds the hard safety caps:
  - If user is PRO: Cap the value at `$250.00`.
  - If user is ELITE: Cap the value at `$500.00`.
- The UI must visually indicate if they have hit the "Max Monthly Payout Cap".

### 3. Enforce the Strict Prop Firm Risk Guardrails
- Create a new, highly visible status card directly below the equity card titled "EVALUATION STATUS".
- Implement state variables tracking three strict rules:
  1. **Daily Drawdown Limit (5%):** Must not lose > $500 in 24 hours.
  2. **Total Drawdown Limit (10%):** Equity must not drop below $9,000.
  3. **Minimum Active Days:** Must execute trades on > 5 separate days.
- If the user violates Rule 1 or Rule 2, their global status changes to a red `❌ ACCOUNT VIOLATED`.
- If the account is Violated, or if Minimum Active Days is not met, the "Request Payout" button MUST be permanently disabled/grayed out for the remainder of the billing cycle.

### 4. Secure State Initialization
- Wrap all equity, profit, and violation calculations in a secure `useMemo` or `useEffect` hook. 
- Ensure that if the database fetch fails, all payout values default to `$0.00` and the button remains locked to prevent glitch-based unauthorized payouts.

EXECUTION REQUIREMENT: The Profile screen must visually separate the $10,000 demo balance from the actual 5%/10% cash payout. The strict 5% daily / 10% total drawdown rules must actively disable the payout button if breached.