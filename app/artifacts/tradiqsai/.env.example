# Expo public environment variables for local dev and EAS builds.
# Copy to .env (gitignored) and paste real values:
#   cp .env.example .env
# For EAS cloud builds, prefer `eas secret:create` or env entries in eas.json.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
